/** Copy to environment.local.ts and fill values. Never commit real keys. */
export const localEnvironment: {
  apiBaseUrl?: string;
  sentryDsn?: string;
  sentryRelease?: string;
} = {
  apiBaseUrl: '',
  sentryDsn: '',
  sentryRelease: '',
};
