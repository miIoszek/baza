# `@baza/api-data-access`

Nest data-access layer (repositories / DB clients).

Empty on purpose — wire Supabase Postgres (or Prisma) providers here later and export them for feature modules in `baza-api`.
